import React from 'react';
import './cartTitle.scss';
import '../../index.scss';

const CartTitle = () => {
	return (
		<div className="cartTitleContainer">
			<div className="heading">Cos de Cumparaturi</div>
		</div>
	);
};

export default CartTitle;
